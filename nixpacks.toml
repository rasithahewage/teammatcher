[phases.build]
cmds = [
  'pip install -r requirements.txt',
  'python manage.py collectstatic --noinput',
  'python manage.py migrate'
]

[start]
cmd = 'gunicorn app_teammatcher.wsgi:application --bind 0.0.0.0:8000'
